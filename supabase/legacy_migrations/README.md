# Legacy migrations

Audit archive only. Do not run, rename, or move these files back into `supabase/migrations`.

The executable replacement chain starts at:

`supabase/migrations/20260723000000_production_baseline.sql`
